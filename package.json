{
  "name": "summit-craft-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && touch dist/.nojekyll",
    "deploy:gh": "npm run build && touch dist/.nojekyll && echo 'Build complete. Push to main branch to deploy to flowstatelabs.ai'"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.2",
    "@astrojs/tailwind": "^6.0.2",
    "@tailwindcss/postcss": "^4.1.11",
    "astro": "^5.12.9",
    "tailwindcss": "^3.4.17"
  }
}
